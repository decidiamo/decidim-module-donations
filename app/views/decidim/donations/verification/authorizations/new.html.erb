<div class="wrapper decidim-donations">
  <div class="row">
    <div class="row">
      <div class="columns large-8 large-centered text-center page-title">
        <h1><%= t(".title") %></h1>
      </div>
      <div class="columns large-4 medium-centered mb-s text-justify">
        <%== t(".instructions") %>
      </div>
    </div>

    <div class="row authorization-form">
      <div class="columns large-4 medium-centered">
        <%= decidim_form_for(@form, url: authorization_path) do |form| %>
          <%= form.hidden_field :handler_handle %>

          <%= form.number_field :amount %>

          <div class="actions">
            <%= form.submit t(".checkout"), class: "button expanded", "data-disable-with" => "#{t('.checkout')}..." %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>

<%= stylesheet_link_tag "decidim/donations/verification" %>
